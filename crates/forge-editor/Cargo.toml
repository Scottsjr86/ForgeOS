[package]
name = "forge-editor"
description = "ForgeOS buffers, parsing, language intelligence, and editor state."
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
forge-bridge = { path = "../forge-bridge" }
forge-core = { path = "../forge-core" }
forge-protocol = { path = "../forge-protocol" }

[lints]
workspace = true
