[package]
name = "forge-world"
description = "ForgeOS Bevy shell, HUD, spatial presentation, and user interaction."
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
forge-core = { path = "../forge-core" }
forge-protocol = { path = "../forge-protocol" }

[lints]
workspace = true
