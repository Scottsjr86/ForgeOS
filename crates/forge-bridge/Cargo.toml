[package]
name = "forge-bridge"
description = "Explicit ForgeOS adapters and ports for real development tools."
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
forge-core = { path = "../forge-core" }
forge-protocol = { path = "../forge-protocol" }

[lints]
workspace = true
