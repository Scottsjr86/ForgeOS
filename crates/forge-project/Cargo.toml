[package]
name = "forge-project"
description = "ForgeOS project registration and persistence adapters."
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
forge-core = { path = "../forge-core" }
forge-protocol = { path = "../forge-protocol" }

[lints]
workspace = true
